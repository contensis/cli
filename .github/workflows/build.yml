name: Node.js CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    # runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '18.x'
      # - name: Install apt packages for build
      #   # The app uses keytar package which requires libsecret and
      #   # an unlocked keyring to be able to store passwords and login.
      #   run:
      #     ${PWD}/packages/contensis-cli/headless-setup.sh
      #     # sudo apt-get update
      #     # sudo apt-get install -y libsecret-1-0 dbus-x11 gnome-keyring
      #     # export $(dbus-launch)
      #     # dbus-launch
      #     # gnome-keyring-daemon --start --daemonize --components=secrets
      #     # echo 'random' | gnome-keyring-daemon -r -d --unlock
      - name: Get npm version
        id: npm-version
        uses: martinbeentjes/npm-get-version-action@main
        with:
          path: packages/contensis-cli
      - name: Build and package
        run: |
          yarn cache clean --mirror
          yarn run bootstrap:ci
          yarn run build
          cd packages/contensis-cli
          npm pack
          npm i -g contensis-cli-${{ steps.npm-version.outputs.current-version}}.tgz
      - name: Test running the cli
        run: |
          contensis-cli push block cli-test-block gitlab.zengenti.com:4567/internal-projects
/migratortron-1000/master/app:latest master -r -cid 9ee20333 -cmsg "test message" -cdt 202
2-11-03T22:34 -author n.flatley@zengenti.com -committer n.flatley@zengenti.com -repo https
://gitlab.zengenti.com/Internal-Projects/migratortron-1000.git -pr GitlabSelfHosted -a zen
hub-dev -p contensis -id 03c8fae0-1aa6-46d3-a0a4-9674fa7f10f1 -s 47a52c7755324debbcc984865
e87200b-35b17f3d39ae40ee9b118358c4dd1666-a516b7c30ebb449ca26e93157588cd46
          contensis-cli get entries -q "externalURL EXISTS" -a trial-006 -p leif2 -id e4376db2-ed3e-49d3-a799-a6ec733a3611 -s a8590f6b4630404186b72fea198d3c12-c6f1f565eb86428ea0170e1b4b8a4b7f-de88eb42e46c4d548a8c36f490f34866
          contensis-cli connect trial-006
          contensis-cli login zengenti 123456
      - name: Archive packaged artifacts
        uses: actions/upload-artifact@v3
        with:
          name: contensis-cli-${{ steps.npm-version.outputs.current-version}}
          path: |
            packages/contensis-cli/dist
            packages/contensis-cli/cli.js
            packages/contensis-cli/shell.js
            packages/contensis-cli/package.json
            packages/contensis-cli/README.md
            packages/contensis-cli/contensis-cli-${{ steps.npm-version.outputs.current-version}}.tgz
