name: Contensis CLI Node and Docker CI

on:
  push:
    branches:
      - main

jobs:
  node:
    uses: ./.github/workflows/build-node.yml
    secrets: inherit

  docker:
    needs: node
    uses: ./.github/workflows/build-docker.yml
    secrets: inherit
